{
  "name": "userale-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm -r run build"
  },
  "devDependencies": {
    "@types/detect-browser": "^4.0.3",
    "pnpm": "^10.0.0"
  },
  "dependencies": {
    "detect-browser": "^5.3.0",
    "esbuild": "^0.21.5"
  }
}